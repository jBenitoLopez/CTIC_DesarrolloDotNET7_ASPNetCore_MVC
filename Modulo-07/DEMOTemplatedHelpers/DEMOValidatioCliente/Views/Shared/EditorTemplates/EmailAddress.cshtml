@model string



@{
    var isRequired = ViewData.ModelMetadata.IsRequired;
    var requiredClass = isRequired ? "form-control-required" : "";
    var validClass = isRequired ? "is-invalid" : "is-valid";
}
@Html.TextBox("", Model, new { @class=$"form-control text-primary {requiredClass}", placeholder = "Enter your Email"})
